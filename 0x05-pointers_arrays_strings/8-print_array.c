#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * print_array - prints an array
 * @a: variable pointer
 * @n: number of arrays
 */

void print_array(int *a, int n)
{
	for (a = 0; a < n; a++)
	{
		printf("%d, ", n[j]);
	}
	printf('\n');
}
